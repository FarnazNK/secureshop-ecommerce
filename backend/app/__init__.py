"""SecureShop API package.

Public surface is intentionally small. External callers should only need:
- `create_app` from `app.main` for the ASGI app
- `get_settings` from `app.core.config` for runtime config
"""
